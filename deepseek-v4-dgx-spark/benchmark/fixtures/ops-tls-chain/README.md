# Synthetic TLS-chain deployment

Certificate metadata is supplied in `snapshots/`. Create `deployment.json` and
`rollback.md` from that metadata alone. No certificate, network, daemon, or
host operation is required or permitted.
