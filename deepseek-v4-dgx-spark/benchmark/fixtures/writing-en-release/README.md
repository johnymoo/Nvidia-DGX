# Release notes task

Create `answer.md` from the task prompt. This fixture intentionally has no valid answer.

Source facts: `REL-3.4.0`; 2026-08-18; `ExportJob`; `export.completed`; API version `2026-06-01`; `legacy_export` removed in 4.0.0; CSV unchanged; update version header and test in staging.
