# Synthetic cgroup memory incident

Use only the captured unit and cgroup event data. Create `service-fixed.unit`
and `diagnosis.json`. The unit is parser input, not a command to run; do not
inspect cgroups, a process manager, or the host.
