# Synthetic backup-schedule incident

The schedule and event capture are synthetic data. Create `backup-fixed.sh` and
`schedule.txt` using only task-local paths and supplied project helpers. The
grader reads the script; it never executes it, cron, or a backup command.
