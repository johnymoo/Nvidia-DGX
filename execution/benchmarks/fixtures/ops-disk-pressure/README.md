# Synthetic capacity-pressure incident

The supplied snapshots are the complete evidence set. Create `triage.json` and
`remediation.md` without examining a filesystem. All paths in the answer must
remain within this task directory and remediation must be bounded and reversible.
