# Working Notes

This directory contains checked but not yet canonical findings: host
inventories, source comparisons, experiments, failed attempts, measurements,
and candidate configurations.

Promote a result to `planning/03-core` only after it has reproducible live
evidence and a documented rollback path.
