# Reference Bundle Example

This sanitized bundle demonstrates packaging only. It contains no measured GPU
performance and cannot enter Best Verified. Validate it with:

```bash
python3 benchmarks/runner/validate_submission.py benchmarks/examples/reference-bundle
```
