# NVIDIA RTX 3090 24 GB

The retained Qwen3.8 profile uses one RTX 3090 with 24 GB VRAM and llama.cpp.
Its receipt is historical and may be reused only when every identity and
invalidation condition matches. The canonical catalog does not infer that a
different board, runtime, quantization, context, or parallelism setting passes.

```bash
./lab list | grep rtx3090-24gb
```
