# DeepSeek V4 Flash / Dual GB10 / Thinking Off Control

Maturity: **Archived**

This is a separate identity for the retained thinking-off Compose control in
the adjacent historical project. It is not the default, does not inherit the
thinking-on receipt or benchmark, and cannot enter Best Verified.

```bash
./run.sh
```

No lifecycle operation is mapped because the repository does not contain an
independently bounded controller for this control profile. Use the
[thinking-on recipe](../dual-dgx-spark-gb10-vllm-flash-0731-patch4-thinking-on/)
for the retained source documentation. Unsupported operations exit non-zero.
