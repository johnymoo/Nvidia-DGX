## 事件摘要

INC-2026-0810 记录 2026年8月10日 的订单 API 事件。09:12 CST 起，该接口的 p95 升至 4.8 秒。排查结果显示，发布 rel-20260810.3 后上游连接池耗尽，导致请求等待时间增加。本复盘用于同步已知事实、已采取的缓解措施和待跟踪工作，避免把当前证据延伸为未验证的结论。

## 影响

从告警出现到恢复期间，共有 1,842 个请求受到影响。09:31 完成回退后，服务开始恢复；09:37 p95 降至 180 毫秒。支持团队可据此回应已记录的请求延迟情况，并将其他现象单独关联到事件记录后再判断。

## 已完成

值班人员已保留发布和回退时间线，确认回退后的延迟指标，并将连接池耗尽这一已知触发条件写入事件材料。当前记录不替代后续验证，也不将本次处置描述为长期修复。

## 后续行动

CR-142 将补充相关负载测试，覆盖连接池压力下的请求行为。CR-143 将补充回退告警，使类似变化更早进入值班视野。两项工作完成后，应更新复盘中的证据和状态。
