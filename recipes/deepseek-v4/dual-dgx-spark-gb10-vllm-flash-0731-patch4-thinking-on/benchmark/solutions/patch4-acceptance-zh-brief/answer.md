2026-08-11 的运行 20260811T002139Z 验证了 deepseek-v4-flash-0731 的 Patch4 配置。该服务在两台主机上以 TP=2 运行，RoCE 链路处于可用状态，NCCL 使用 NET/IB。正确性、工具调用、代理行为、并发测试以及 40 分钟 c4 soak 均已通过。期间共完成 621 个请求，生成 223,268 个 token，未观察到空响应、乱码响应或 HTTP 错误。验收记录同时保留两台主机的运行状态、接口检查和日志证据，以便复核分布式进程及其恢复边界。该结果只描述本次冻结配置下的受控验证，不替代后续变更前的检查。后续运行仍应按既定启动、状态检查、停止和恢复流程执行，并在服务切换前确认受保护工作负载状态未发生变化。任何新的运行参数、模型版本或资源配置都需要单独记录并重新验证。
