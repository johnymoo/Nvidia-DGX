# Synthetic unit restart incident

Read the supplied unit and snapshots only. Create `service-fixed.unit` and
`diagnosis.json`; both are consumed by local parsers. Do not start, stop, or
inspect a real service manager.
