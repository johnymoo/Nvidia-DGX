# Synthetic database-pool incident

Use only the metrics snapshot. Create `tuning.json` and `rollback.md`; the plan
is not applied to any database. Keep all paths local and conclusions bounded by
the supplied evidence.
