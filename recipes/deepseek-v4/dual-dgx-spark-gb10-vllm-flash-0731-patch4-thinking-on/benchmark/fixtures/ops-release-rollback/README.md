# Synthetic release rollback

The files under `snapshots/` define a local release layout. Create
`rollback.sh` and `rollback-plan.json`. The grader reads shell text only; do not
run it, contact a service, or access paths outside this task directory.
