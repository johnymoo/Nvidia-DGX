# Synthetic reverse-proxy incident

Only the files in this task directory are incident evidence. `snapshots/` shows
the failed fragment and captured events. Create `fix.conf` and `diagnosis.json`
as described by the benchmark task. These files are parsed locally; do not run
or contact any proxy, daemon, network endpoint, or host service.
