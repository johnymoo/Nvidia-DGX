upstream ledger_backend {
    server ledger-a:9000;
    server ledger-b:9000;
}

server {
    location /api/ {
        proxy_pass http://ledger_backend;
        proxy_connect_timeout 60s;
        proxy_read_timeout 5s;
    }
}
