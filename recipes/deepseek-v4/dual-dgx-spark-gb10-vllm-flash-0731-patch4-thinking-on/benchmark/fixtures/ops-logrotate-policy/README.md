# Synthetic log-rotation incident

Use the supplied policy and event capture as the complete evidence set. Create
`logrotate-fixed.conf` and `diagnosis.json`. The configuration is checked as
text and is never passed to logrotate or a daemon.
